# hyprpaper v0.8.x uses the block form; `monitor` must be the first key.
# Empty monitor = applies to all outputs. Default scaling fills/covers the screen.
preload = ~/background.png

wallpaper {
  monitor =
  path = ~/background.png
}

ipc = off
splash = false
